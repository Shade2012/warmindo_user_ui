import 'package:flutter/material.dart';

class GuestMenuPage extends StatelessWidget {
  const GuestMenuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Guest Menu Page'),
      )
    );
  }
}