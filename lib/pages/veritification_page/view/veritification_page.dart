import 'package:flutter/material.dart';

class VeritificationPage extends StatelessWidget {
  const VeritificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Veritification Page'),
    ));
  }
}